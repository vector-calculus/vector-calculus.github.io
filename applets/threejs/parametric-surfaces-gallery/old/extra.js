export function testing(x, y) {

    var z1 = mul(exp(complex(0, 2 * pi * k1 / n)), pow(cos(complex(x, y)), 2 / n));
    var z2 = mul(exp(complex(0, 2 * pi * k2 / n)), pow(sin(complex(x, y)), 2 / n));

    return [z1.re, z2.re, z1.im * cos(a) + z2.im * sin(a)];

  }